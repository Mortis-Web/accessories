const goTopBtn=document.querySelector(".go-top-btn");function checkHeight(){if(window.scrollY>200){goTopBtn.style.bottom="35px"}else{goTopBtn.style.bottom="-100%"}}goTopBtn.addEventListener("click",()=>{window.scrollTo({top:0,behavior:"smooth"})});let nums=document.querySelectorAll(".nums .num");let section=document.querySelector(".three");let started=false;function handleCounterScroll(){if(window.scrollY+200>=section.offsetTop-100){if(!started){nums.forEach(num=>startCount(num));started=true}}}function startCount(el){let goal=el.dataset.goal;let count=setInterval(()=>{el.textContent++;if(el.textContent==goal){clearInterval(count)}},2000/goal)}let scrollTimeout;window.addEventListener("scroll",()=>{clearTimeout(scrollTimeout);scrollTimeout=setTimeout(()=>{checkHeight();handleCounterScroll()},50)});document.querySelectorAll(".feature-wrapper").forEach(wrapper=>{const card=wrapper.querySelector(".feature

